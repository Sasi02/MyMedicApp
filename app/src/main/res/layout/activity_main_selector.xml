<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:weightSum="10"
    tools:context="com.example.user.mymedic.MainSelector">

    <RelativeLayout
        android:layout_weight="2"
        android:layout_width="match_parent"
        android:layout_height="0dp">

        <fragment
            android:name="com.example.user.mymedic.TopBar"
            android:id="@+id/top_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:layout="@layout/activity_top_bar" />

    </RelativeLayout>

    <LinearLayout
        android:columnCount="1"
        android:rowCount="2"
        android:alignmentMode="alignMargins"
        android:layout_weight="8"
        android:columnOrderPreserved="false"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="10dp"
        android:weightSum="2"
        android:orientation="vertical">

        <ImageButton
            android:id="@+id/patientButton"
            android:scaleType="fitCenter"
            android:padding="50dp"
            android:backgroundTint="@color/secondaryColor"
            android:elevation="2dp"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:srcCompat="@mipmap/ic_patient_selection"
            android:layout_below="@+id/patient_top_bar"
            android:layout_weight="1" />

        <ImageButton
            android:id="@+id/phamacistButton"
            android:scaleType="fitCenter"
            android:padding="50dp"
            android:backgroundTint="@color/secondaryColor"
            android:elevation="2dp"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:srcCompat="@mipmap/ic_phamacist_selection"
            android:layout_below="@+id/imageButton"
            android:layout_weight="1"  />
    </LinearLayout>


</LinearLayout>
